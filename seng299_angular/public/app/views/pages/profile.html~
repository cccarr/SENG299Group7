<div class="page-header">
	<h1 ng-if="user.type == 'create'">Create User</h1>
	<h1 ng-if="user.type == 'edit'" >Welcome, {{ user.userData.name }} </h1>
</div>

<form class="form-horizontal" ng-submit="user.saveUser()">
	
	<div class="form-group" ng-if="user.type== 'edit'">
			<input type="hidden" class="form-control" ng-model="user.userData.user_id" >
	</div>

	<div class="form-group">
		<label class="col-sm-2 control-label">Name</label>
		<div class="col-sm-6">
			<input type="text" class="form-control" ng-model="user.userData.name">
		</div>
	</div>

	<div class="form-group">
		<label class="col-sm-2 control-label">Username</label>
		<div class="col-sm-6">
			<input type="text" class="form-control" ng-model="user.userData.username">
		</div>
	</div>

	<div class="form-group">
		<label class="col-sm-2 control-label">Password</label>
		<div class="col-sm-6">
			<input type="password" class="form-control" ng-model="user.userData.password">
		</div>
	</div>
	
	<div class="form-group">
	<div class="col-sm-offset-2 col-sm-6">
		<button type="submit" class="btn btn-success btn-lg btn-block" ng-if="user.type == 'create'">Create User</button>
		<button type="submit" class="btn btn-success btn-lg btn-block" ng-if="user.type == 'edit'">Update Profile</button>
	</div>
	</div>
	<p>

	</p>
	<h3> Your stats: </h3>
	<p>
	
	Time until ban ends:	{{user.userData.dt_ban_end}} 
	

	</p>
</form>

	<h3> Reservations </h3>

	<table class="table table-bordered table-striped">
	<thead>
		<tr>
		
			<th>Booth</th>
			<th>User</th>
			<th>Date Start</th>
			
		</tr>
	</thead>
	<tbody>
		<tr ng-repeat="booking in reservation.reservations">
	
			<td>{{ booking.booth_title }}</td>
			<td>{{ booking.user_id }}</td>
			<td>{{ booking.dt_start }}</td>
			<td class="col-sm-2">
				<a href="#" ng-click="reservation.deleteReservation(booking._id)" class="btn btn-primary">Delete</a>
			</td>
			<td class="col-sm-2">
				<a href="/reservations/{{booking._id}}" class="btn btn-primary">Edit</a>
			</td>

		</tr>
	</tbody>
</table>

<div class="row show-hide-message" ng-show="user.message">
	<div class="col-sm-6 col-sm-offset-2">
		
		<div class="alert alert-info">
			{{ user.message }}
		</div>

	</div>
</div>
